#Complete End-to-End Medical AI Assistant Codebase

#Using Streamlit + FastAPI + LangChain + FAISS + Ollama + UV

#Service URL
🎨 Streamlit UI http://localhost:8501
⚙️ FastAPI Docs http://localhost:8000/docs

docker-compose up --build -d
